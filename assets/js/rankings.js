fetch('data/rankings.json').then(r=>r.json()).then(data=>{document.querySelector('#rankTable').innerHTML=data.map(p=>`<article class="rank-row card reveal ${p.rank===1?'top1':''}"><div class="rank-number">${p.rank}</div><div class="avatar">${initials(p.name)}</div><div class="player-name"><b>${p.flag} ${p.name}</b><small>${p.country}</small></div><div class="points"><strong>${p.points}</strong><small class="muted"> очков</small></div><div class="movement ${p.movement>0?'up':p.movement<0?'down':'same'}">${p.movement>0?'▲ +'+p.movement:p.movement<0?'▼ '+p.movement:'—'}</div><div class="titles">🏆 ${p.titles} титула</div><div class="form">${p.form.map(v=>`<i class="${v?'w':'l'}"></i>`).join('')}</div></article>`).join('');document.querySelectorAll('.reveal').forEach(x=>x.classList.add('visible'))});
